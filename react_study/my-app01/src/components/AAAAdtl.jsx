export const AAAAdtl =(props)=>{
    const aaaadtl={
        name:props.name,
        age:props.age,
        score:props.score,
        aaaadtl:props.aaaadtl
    }
    return (
        <>
        <p>{props.name} {props.age} {props.score} </p>
        </>
    );
}